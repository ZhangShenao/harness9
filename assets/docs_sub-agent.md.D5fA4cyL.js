import{_ as t,o as s,c as n,a2 as a}from"./chunks/framework.CNfFRqmr.js";const g=JSON.parse(`{"title":"Sub-Agent System Implementation Principles","description":"harness9's Sub-Agent system lets the main agent delegate clearly bounded subtasks to specialized agents with independ...","frontmatter":{"title":"Sub-Agent System Implementation Principles","description":"harness9's Sub-Agent system lets the main agent delegate clearly bounded subtasks to specialized agents with independ..."},"headers":[],"relativePath":"docs/sub-agent.md","filePath":"docs/sub-agent.md"}`),i={name:"docs/sub-agent.md"};function o(d,e,l,r,p,c){return s(),n("div",null,[...e[0]||(e[0]=[a(`<h1 id="sub-agent-system-implementation-principles" tabindex="-1">Sub-Agent System Implementation Principles <a class="header-anchor" href="#sub-agent-system-implementation-principles" aria-label="Permalink to &quot;Sub-Agent System Implementation Principles&quot;">​</a></h1><p>harness9&#39;s Sub-Agent system lets the main agent delegate <strong>clearly bounded subtasks</strong> to specialized agents with independent context, a restricted toolset, and optional model override. A Sub-Agent is not a new abstraction — it is simply a plain <code>engine.AgentEngine</code> instance running on an isolated Session, reusing the existing <code>RunStream</code> pipeline without changing a single line of the core <code>runLoop</code>.</p><hr><h2 id="system-architecture" tabindex="-1">System Architecture <a class="header-anchor" href="#system-architecture" aria-label="Permalink to &quot;System Architecture&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>internal/subagent/</span></span>
<span class="line"><span>├── definition.go   # SubAgentDefinition struct + ResolveTools + Validate</span></span>
<span class="line"><span>├── registry.go     # Registry: Register / Get / List (registered at startup, read-only at runtime)</span></span>
<span class="line"><span>├── frontmatter.go  # parseAgentFile: YAML frontmatter + body -&gt; SubAgentDefinition</span></span>
<span class="line"><span>├── loader.go       # Registry.LoadFromDir: scans .harness9/agents/*.md file-based definitions</span></span>
<span class="line"><span>├── prompt.go       # promptBuilder: Sub-Agent system prompt + Skills preloading + workDir injection</span></span>
<span class="line"><span>├── tracker.go      # TaskTracker: single source of truth for background tasks (Start/AppendLog/Finish/DrainCompleted/List/Get)</span></span>
<span class="line"><span>├── runner.go       # Runner: builds an isolated sub-engine + runs RunStream + bridges approval and progress</span></span>
<span class="line"><span>└── task_tool.go    # TaskTool: the sole delegation entry point called by the main agent (tools.BaseTool)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>cmd/harness9/</span></span>
<span class="line"><span>├── main.go         # Wiring: registers built-in general-purpose, LoadFromDir, NewRunner, NewTaskTool</span></span>
<span class="line"><span>├── tui_update.go   # EventSubAgent rendering + TaskTracker.DrainCompleted injection in dispatch() + @agent direct run + task panel keybindings</span></span>
<span class="line"><span>└── tui_view.go     # renderSubAgentProgress(), renderTaskPanel(), background task status segment in renderStatusBar()</span></span></code></pre></div><hr><h2 id="sub-agent-definitions" tabindex="-1">Sub-Agent Definitions <a class="header-anchor" href="#sub-agent-definitions" aria-label="Permalink to &quot;Sub-Agent Definitions&quot;">​</a></h2><h3 id="built-in-general-purpose-sub-agent" tabindex="-1">Built-in general-purpose Sub-Agent <a class="header-anchor" href="#built-in-general-purpose-sub-agent" aria-label="Permalink to &quot;Built-in general-purpose Sub-Agent&quot;">​</a></h3><p>harness9 ships a built-in <strong><code>general-purpose</code> Sub-Agent</strong>, deliberately designed to match the same-named capability in two mainstream frameworks:</p><ul><li><strong>Claude Code</strong>&#39;s <a href="https://code.claude.com/docs/en/sub-agents#general-purpose" target="_blank" rel="noreferrer">general-purpose subagent</a>: &quot;A capable agent for complex, multi-step tasks that require both exploration and action&quot;, inheriting all tools and the model of the main conversation — the fallback delegation target for when &quot;no more specialized Sub-Agent&quot; exists.</li><li><strong>DeepAgents</strong>&#39; <a href="https://docs.langchain.com/oss/python/deepagents/subagents#the-general-purpose-subagent" target="_blank" rel="noreferrer">general-purpose subagent</a>: every deep agent carries one by default, for scenarios that need &quot;context isolation without specialized behavior&quot; — the main agent delegates a whole multi-step task and gets back only a concise conclusion, avoiding polluting the main context with intermediate steps.</li></ul><p>Both share the same design core, which harness9 fully inherits:</p><table tabindex="0"><thead><tr><th>Dimension</th><th>general-purpose value</th><th>Meaning</th></tr></thead><tbody><tr><td><code>Tools</code></td><td>Empty (nil)</td><td><strong>Inherits all tools available to the parent agent</strong> — can read/write files, execute commands, invoke skills</td></tr><tr><td><code>Model</code></td><td>Empty (<code>&quot;&quot;</code>)</td><td><strong>Inherits the parent agent&#39;s model</strong>, no override</td></tr><tr><td><code>MaxTurns</code></td><td>Empty (0)</td><td>Inherits the engine&#39;s default turn count (same as the main agent)</td></tr><tr><td>Positioning</td><td>Fallback delegation target</td><td>Use when the task is clearly bounded, can be completed independently, and you want context isolation</td></tr></tbody></table><p><strong>When to delegate to it</strong>: the task requires both exploration and modification, needs complex reasoning to explain intermediate results, or involves multiple interdependent steps, and you only want the final conclusion rather than the verbose intermediate process.</p><h3 id="programmatic-definition" tabindex="-1">Programmatic Definition <a class="header-anchor" href="#programmatic-definition" aria-label="Permalink to &quot;Programmatic Definition&quot;">​</a></h3><p>The built-in <code>general-purpose</code> is constructed directly in <code>main.go</code> and registered into <code>subagent.Registry</code>:</p><div class="language-go vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">go</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">subAgentReg.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Register</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">subagent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">SubAgentDefinition</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Name:         </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;general-purpose&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Description:  </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;General-purpose Sub-Agent for tasks requiring both exploration and modification, complex reasoning, or multi-step dependencies. Use when the task boundary is clear, can be completed independently, and you want context isolation (only the final conclusion returned instead of the verbose intermediate process); it is the default fallback choice when no more specialized Sub-Agent is available. Inherits all tools and the model available to the parent agent.&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    SystemPrompt: generalPurposeSystemPrompt, </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Emphasizes &quot;context isolation + self-contained conclusion&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Source:       </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;builtin&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Tools/Model/MaxTurns all left empty: tools and model inherit from parent, turn count inherits engine default</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span></code></pre></div><blockquote><p>When more specialized capabilities are needed (e.g. security auditing, documentation writing), prefer adding a new Sub-Agent via the <strong>file-based definition</strong> described below, instead of stacking more programmatic built-ins — keep the core minimal and leave specialized roles to the project side.</p></blockquote><h3 id="subagentdefinition-field-reference" tabindex="-1">SubAgentDefinition Field Reference <a class="header-anchor" href="#subagentdefinition-field-reference" aria-label="Permalink to &quot;SubAgentDefinition Field Reference&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>Name</code></td><td><code>string</code></td><td>Unique identifier, must match <code>^[a-z0-9][a-z0-9-]*$</code></td></tr><tr><td><code>Description</code></td><td><code>string</code></td><td>The &quot;when to use me&quot; text written for the LLM; the core basis on which the <code>task</code> tool schedules</td></tr><tr><td><code>SystemPrompt</code></td><td><code>string</code></td><td>Sub-Agent system prompt body</td></tr><tr><td><code>Tools</code></td><td><code>[]string</code></td><td>Tool allowlist; nil/empty = inherit all tools available to the parent</td></tr><tr><td><code>DisallowedTools</code></td><td><code>[]string</code></td><td>Tool denylist (deny first, then allow)</td></tr><tr><td><code>Model</code></td><td><code>string</code></td><td>Model override; <code>&quot;&quot;</code> = inherit the parent agent&#39;s model</td></tr><tr><td><code>MaxTurns</code></td><td><code>int</code></td><td>Maximum turn count; <code>0</code> = inherit the default (same as the main agent, currently 50)</td></tr><tr><td><code>Skills</code></td><td><code>[]string</code></td><td>Skill names to preload at startup (body injected into the Sub-Agent system prompt)</td></tr><tr><td><code>Source</code></td><td><code>string</code></td><td>Diagnostic field: <code>&quot;builtin&quot;</code> or a file path</td></tr></tbody></table><h3 id="file-based-definition" tabindex="-1">File-Based Definition <a class="header-anchor" href="#file-based-definition" aria-label="Permalink to &quot;File-Based Definition&quot;">​</a></h3><p>Create a <code>*.md</code> file under <code>.harness9/agents/</code> in the working directory; harness9 scans and loads it automatically at startup. <strong>A file-based definition overrides a programmatic definition of the same name</strong> (logged, no error). If the file has no <code>name</code> field, it falls back to the filename (with the <code>.md</code> suffix stripped) as the Name.</p><p><strong>Complete example <code>.harness9/agents/security-auditor.md</code></strong>:</p><div class="language-markdown vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">markdown</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">---</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">security-auditor</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">description</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">Security audit expert. Use after code changes involving authentication, authorization, or input validation to detect OWASP Top 10 vulnerabilities.</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">tools</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">read_file, bash</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">disallowed_tools</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">write_file, edit_file</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">model</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">openai/gpt-4o</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">max_turns</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">30</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">skills</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">security-review</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">---</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">You are an application security engineer focused on identifying security vulnerabilities in code.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">When reviewing, output findings prioritized as: Critical &gt; High &gt; Medium &gt; Low, each with a CWE number and a remediation suggestion.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Do not modify files; only output the review report.</span></span></code></pre></div><p><strong>Frontmatter field quick reference</strong>:</p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>name</code></td><td>string</td><td>Same as SubAgentDefinition.Name</td></tr><tr><td><code>description</code></td><td>string</td><td>Same as SubAgentDefinition.Description</td></tr><tr><td><code>tools</code></td><td>comma-separated string</td><td>Allowlist, e.g. <code>read_file, bash</code></td></tr><tr><td><code>disallowed_tools</code></td><td>comma-separated string</td><td>Denylist</td></tr><tr><td><code>model</code></td><td>string</td><td>Model override</td></tr><tr><td><code>max_turns</code></td><td>int</td><td>Maximum turn count</td></tr><tr><td><code>skills</code></td><td>comma-separated string</td><td>Names of skills to preload</td></tr></tbody></table><hr><h2 id="the-task-tool" tabindex="-1">The task Tool <a class="header-anchor" href="#the-task-tool" aria-label="Permalink to &quot;The task Tool&quot;">​</a></h2><p><code>task</code> is a plain tool (<code>tools.BaseTool</code>) registered in the parent agent&#39;s tool registry. The LLM calls the <code>task</code> tool to delegate subtasks; a Sub-Agent&#39;s registry never includes <code>task</code>, categorically prohibiting recursion.</p><h3 id="tool-parameters" tabindex="-1">Tool Parameters <a class="header-anchor" href="#tool-parameters" aria-label="Permalink to &quot;Tool Parameters&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Parameter</th><th>Type</th><th style="text-align:center;">Required</th><th>Description</th></tr></thead><tbody><tr><td><code>subagent_type</code></td><td>string (enum)</td><td style="text-align:center;">Yes</td><td>The Name of a registered Sub-Agent, dynamically enumerated by <code>Definition()</code></td></tr><tr><td><code>prompt</code></td><td>string</td><td style="text-align:center;">Yes</td><td>The complete task description passed to the Sub-Agent. The Sub-Agent cannot see the parent&#39;s conversation history, so all necessary information must be written here</td></tr><tr><td><code>description</code></td><td>string</td><td style="text-align:center;">No</td><td>A short 3-5 word title (for UI display)</td></tr><tr><td><code>background</code></td><td>bool</td><td style="text-align:center;">No</td><td>Whether to run asynchronously in the background (default <code>false</code>)</td></tr></tbody></table><p><code>Definition()</code> is <strong>dynamically generated</strong> on each invocation, enumerating the Names of all registered Sub-Agents as the <code>enum</code> for <code>subagent_type</code>, with their Descriptions concatenated into the tool description — this is the basis on which the LLM chooses &quot;which Sub-Agent to call&quot;:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Delegate a clearly bounded task to a specialized Sub-Agent. The Sub-Agent has independent context and a restricted toolset.</span></span>
<span class="line"><span>Available Sub-Agents:</span></span>
<span class="line"><span>- general-purpose: General-purpose Sub-Agent for tasks requiring both exploration and modification, complex reasoning, or multi-step dependencies. Use when the task is clearly bounded, can be completed independently, and context isolation is desired; it is the default fallback choice when no more specialized Sub-Agent is available. Inherits all tools and the model available to the parent agent.</span></span>
<span class="line"><span>- security-auditor: Security audit expert. ... (file-based definition)</span></span></code></pre></div><h3 id="foreground-execution-background-false-default" tabindex="-1">Foreground Execution (<code>background=false</code>, default) <a class="header-anchor" href="#foreground-execution-background-false-default" aria-label="Permalink to &quot;Foreground Execution (\`background=false\`, default)&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>task tool call</span></span>
<span class="line"><span>    │ execCtx = the calling context of the parent</span></span>
<span class="line"><span>    ▼</span></span>
<span class="line"><span>Runner.Run(..., background=false)</span></span>
<span class="line"><span>    │ Builds an isolated sub-engine, calls RunStream, consumes the event stream</span></span>
<span class="line"><span>    │ Approval request → parentApproval(ctx, ...) → forwarded to the parent&#39;s TUI approval dialog</span></span>
<span class="line"><span>    ▼</span></span>
<span class="line"><span>Blocks until the sub-engine&#39;s channel closes</span></span>
<span class="line"><span>    │</span></span>
<span class="line"><span>    ▼</span></span>
<span class="line"><span>Returns &lt;task state=&quot;completed&quot;&gt;&lt;task_result&gt;...final text...&lt;/task_result&gt;&lt;/task&gt;</span></span></code></pre></div><p>The tool result of foreground execution is injected directly as the tool call&#39;s Output into the parent agent&#39;s context history, so the main agent can immediately read the Sub-Agent&#39;s output.</p><h3 id="background-execution-background-true" tabindex="-1">Background Execution (<code>background=true</code>) <a class="header-anchor" href="#background-execution-background-true" aria-label="Permalink to &quot;Background Execution (\`background=true\`)&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>task tool call</span></span>
<span class="line"><span>    │</span></span>
<span class="line"><span>    ▼</span></span>
<span class="line"><span>The task tool immediately returns &lt;task id=&quot;task-general-purpose-1&quot; state=&quot;running&quot;/&gt;</span></span>
<span class="line"><span>    │</span></span>
<span class="line"><span>    ▼ Simultaneously: go func(){...}()</span></span>
<span class="line"><span>        execCtx is derived from the session-level baseCtx (independent of the parent turn, unaffected by the tool&#39;s 60s timeout)</span></span>
<span class="line"><span>        Approval requests → always denied (fail-closed), returning &quot;no approval channel available for the Sub-Agent, automatically denied&quot;</span></span>
<span class="line"><span>        Sub-engine event stream → tracker.AppendLog(id, update) (full-process logs written to memory, locked, not routed through a channel)</span></span>
<span class="line"><span>        Sub-engine execution completes → tracker.Finish(id, finalText, isErr)</span></span>
<span class="line"><span>            │ Triggers the SetNotify callback → tea.Program.Send(subAgentNotifyMsg) → TUI instantly displays a completion notice</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Before the next dispatch():</span></span>
<span class="line"><span>    tracker.DrainCompleted() → prepended to the prompt → injected into the LLM context</span></span></code></pre></div><hr><h2 id="execution-model-and-context-propagation" tabindex="-1">Execution Model and Context Propagation <a class="header-anchor" href="#execution-model-and-context-propagation" aria-label="Permalink to &quot;Execution Model and Context Propagation&quot;">​</a></h2><h3 id="the-two-phase-execution-of-runner" tabindex="-1">The Two-Phase Execution of Runner <a class="header-anchor" href="#the-two-phase-execution-of-runner" aria-label="Permalink to &quot;The Two-Phase Execution of Runner&quot;">​</a></h3><p><code>Runner.Run</code> is the core of Sub-Agent execution:</p><ol><li><strong>Build an isolated registry</strong>: <code>buildChildRegistry</code> filters tools by <code>ResolveTools</code> (allowlist ∩ full set - denylist - task), wrapped with <code>permission.NewFileHook</code> (inheriting the same <code>settings.json</code>) + <code>denyTaskHook</code> (anti-recursion) + sharedHooks (dangerHook + offloadHook).</li><li><strong>Resolve the Provider</strong>: when <code>def.Model != &quot;&quot;</code>, a new OpenAI Provider is created and its context window is looked up; when <code>&quot;&quot;</code>, the parent agent&#39;s model is reused.</li><li><strong>Build the PromptBuilder</strong>: Sub-Agent system prompt + workDir injection + the body of skills listed in <code>def.Skills</code> (loaded via <code>skills.Index.GetFullContent</code>, silently ignored on failure).</li><li><strong>Independent MemorySession</strong>: <code>memory.NewMemorySession(childID)</code> (in-memory only, containing neither the parent&#39;s conversation history nor the parent&#39;s system prompt).</li><li><strong>Start RunStream</strong>: <code>sub.RunStream(execCtx, prompt)</code>, consuming the event stream, forwarding progress, bridging approvals, and accumulating the final text.</li></ol><h3 id="context-propagation-rules" tabindex="-1">Context Propagation Rules <a class="header-anchor" href="#context-propagation-rules" aria-label="Permalink to &quot;Context Propagation Rules&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Main agent ──► task tool ──► prompt string ──► Sub-Agent (the only source of information)</span></span>
<span class="line"><span>Sub-Agent ──► FinalText ──► tool result ──► parent agent&#39;s context (foreground)</span></span>
<span class="line"><span>Sub-Agent ──► TaskTracker ──► DrainCompleted ──► prepended to the parent agent&#39;s next prompt (background)</span></span></code></pre></div><p>The Sub-Agent <strong>cannot see</strong> the parent agent&#39;s conversation history or system prompt. File paths, background information, and requirement details must be passed explicitly through the <code>task</code> tool&#39;s <code>prompt</code> parameter.</p><h3 id="execution-context-differences" tabindex="-1">Execution Context Differences <a class="header-anchor" href="#execution-context-differences" aria-label="Permalink to &quot;Execution Context Differences&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Dimension</th><th>Foreground (<code>background=false</code>)</th><th>Background (<code>background=true</code>)</th></tr></thead><tbody><tr><td>execCtx source</td><td>The parent caller&#39;s <code>ctx</code> (within the 60s tool timeout)</td><td>Derived from the session-level <code>baseCtx</code> (independent of the parent turn)</td></tr><tr><td>Approval policy</td><td>Forwards the parent&#39;s <code>ApprovalFunc</code>, TUI approval dialog available</td><td>Always denied (fail-closed)</td></tr><tr><td>Result delivery</td><td>tool result returned synchronously</td><td><code>TaskTracker.Finish</code> writes to memory; injected via <code>DrainCompleted</code> at the next dispatch</td></tr><tr><td>Progress log</td><td>Rendered live to subAgentLines via <code>EventSubAgent</code></td><td>Buffered in memory via <code>TaskTracker.AppendLog</code>, viewable via the <code>/tasks</code> panel</td></tr><tr><td>Cancellation propagation</td><td>Parent ctx cancellation → Sub-Agent cancelled accordingly</td><td>Cancelled only when baseCtx is cancelled (process shutdown)</td></tr></tbody></table><hr><h2 id="tui-live-progress-rendering" tabindex="-1">TUI Live Progress Rendering <a class="header-anchor" href="#tui-live-progress-rendering" aria-label="Permalink to &quot;TUI Live Progress Rendering&quot;">​</a></h2><p>While a foreground Sub-Agent is executing, the TUI appends <code>[agent-name]</code>-prefixed dark-cyan progress lines in real time below the tool progress area:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>  [general-purpose] Sub-Agent starting...</span></span>
<span class="line"><span>  [general-purpose] ▸ read_file</span></span>
<span class="line"><span>  [general-purpose]   ✓</span></span>
<span class="line"><span>  [general-purpose] ▸ bash</span></span>
<span class="line"><span>  [general-purpose]   ✓</span></span>
<span class="line"><span>  [general-purpose] Root cause located, summarizing conclusion...</span></span>
<span class="line"><span>  [general-purpose] ✓ Done</span></span></code></pre></div><p>At most <code>maxSubAgentLines = 12</code> of the most recent progress lines are retained, preventing unbounded growth from a long-running Sub-Agent. <code>SubAgentThinking</code> (reasoning delta) is intentionally not displayed, to reduce noise.</p><p>Progress data flow: <code>Runner.emit(SubAgentUpdate)</code> → <code>hooks.SubAgentProgressFunc</code> (injected into context) → <code>RunStream</code> converts to <code>EventSubAgent</code> event → TUI <code>EventSubAgent</code> case → appended to <code>m.subAgentLines</code>.</p><hr><h2 id="security-guarantees" tabindex="-1">Security Guarantees <a class="header-anchor" href="#security-guarantees" aria-label="Permalink to &quot;Security Guarantees&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Security layer</th><th>Mechanism</th><th>Description</th></tr></thead><tbody><tr><td>Anti-recursion</td><td>The child registry never includes the <code>task</code> tool</td><td><code>ResolveTools</code> hardcodes <code>denied[&quot;task&quot;]=true</code></td></tr><tr><td>Anti-recursion (defense in depth)</td><td><code>denyTaskHook.BeforeExecute</code></td><td>Double defense: even if future code introduces <code>task</code>, the hook will still deny it</td></tr><tr><td>No privilege escalation</td><td>Inherits the same <code>.harness9/settings.json</code></td><td><code>permission.NewFileHook(settingsPath)</code> reuses the same rules file</td></tr><tr><td>Permissions only additively stricter</td><td>Sub-Agent additionally layers on DisallowedTools + denyTaskHook</td><td>Can only be more restricted than the parent, never more permissive</td></tr><tr><td>Context isolation</td><td>Independent <code>MemorySession</code> (in-memory only)</td><td>Contains neither the parent&#39;s conversation history nor the parent&#39;s system prompt — no data leak path</td></tr><tr><td>Tool isolation</td><td><code>ResolveTools</code> (allowlist ∩ full set - denylist - task)</td><td>Only explicitly allowed tool instances are registered</td></tr><tr><td>Background approval fail-closed</td><td>Background Sub-Agent approvals are always denied</td><td>Without a TUI channel, dangerous operations are denied rather than auto-approved</td></tr><tr><td>Sensitive paths</td><td>sharedHooks includes <code>dangerHook</code></td><td>19 high-risk patterns (<code>~/.ssh</code>, <code>~/.aws</code>, etc.) protect Sub-Agents as well</td></tr></tbody></table><hr><h2 id="tasktracker-—-single-source-of-truth-for-background-tasks" tabindex="-1">TaskTracker — Single Source of Truth for Background Tasks <a class="header-anchor" href="#tasktracker-—-single-source-of-truth-for-background-tasks" aria-label="Permalink to &quot;TaskTracker — Single Source of Truth for Background Tasks&quot;">​</a></h2><p><code>TaskTracker</code> is the thread-safe single source of truth for background Sub-Agent tasks, replacing the old <code>Mailbox</code>, and taking on both full-process log buffering and result injection responsibilities:</p><h3 id="api-overview" tabindex="-1">API Overview <a class="header-anchor" href="#api-overview" aria-label="Permalink to &quot;API Overview&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Method</th><th>Caller</th><th>Description</th></tr></thead><tbody><tr><td><code>Start(agentName, prompt) string</code></td><td>When a background goroutine starts</td><td>Registers a Running task, returns a unique <code>id</code> (format <code>task-{agent}-{seq}</code>)</td></tr><tr><td><code>AppendLog(id, SubAgentUpdate)</code></td><td>While the background goroutine streams progress</td><td>Appends the progress event to the in-memory buffer (locked), not routed through any channel</td></tr><tr><td><code>Finish(id, finalText, isErr)</code></td><td>When the background goroutine completes</td><td>Marks Done/Failed, triggers the <code>SetNotify</code> callback (called outside the lock)</td></tr><tr><td><code>DrainCompleted() []CompletedTask</code></td><td>Before TUI <code>dispatch()</code></td><td>Returns completed-but-not-yet-injected results, marking them as injected (idempotent)</td></tr><tr><td><code>List() []TaskSnapshot</code></td><td>TUI task panel</td><td>Full snapshot, in creation order</td></tr><tr><td><code>Get(id) (TaskDetail, bool)</code></td><td>TUI task detail</td><td>Returns a <code>TaskDetail</code> with a deep copy of the full-process log</td></tr><tr><td><code>RunningCount() int</code></td><td>TUI status bar</td><td>Number of running tasks</td></tr><tr><td><code>DoneCount() int</code></td><td>TUI status bar</td><td>Number of finished (completed + failed) tasks</td></tr><tr><td><code>SetNotify(fn func())</code></td><td>At TUI initialization</td><td>Registers the completion notification callback</td></tr></tbody></table><h3 id="two-independent-paths" tabindex="-1">Two Independent Paths <a class="header-anchor" href="#two-independent-paths" aria-label="Permalink to &quot;Two Independent Paths&quot;">​</a></h3><p><strong>Injection path</strong>: <code>Finish</code> writes the final text to memory; on the parent agent&#39;s <strong>next dispatch</strong>, <code>DrainCompleted</code> drains it and prepends it into the LLM context (the <code>pendingSubAgentInject</code> buffer). <code>DrainCompleted</code> is idempotent — an already-injected result will not be taken again.</p><p><strong>Notification path</strong>: <code>Finish</code> also triggers the <code>SetNotify</code> callback — at startup the TUI registers it as <code>tea.Program.Send(subAgentNotifyMsg{})</code>, and the moment a background task completes, a &quot;✓ background Sub-Agent completed&quot; notice is appended to the scrollback (display only, does not consume the injection buffer; the two paths do not interfere with each other).</p><p><strong>Full-process log</strong>: <code>AppendLog</code> writes directly to the in-memory buffer (locked), never going through a channel at all, fundamentally eliminating the risk of send-on-closed-channel. The log is exposed to the <code>/tasks</code> panel detail view via <code>Get(id).Log</code>.</p><hr><h2 id="background-task-viewer" tabindex="-1">Background Task Viewer <a class="header-anchor" href="#background-task-viewer" aria-label="Permalink to &quot;Background Task Viewer&quot;">​</a></h2><h3 id="status-bar-indicator" tabindex="-1">Status Bar Indicator <a class="header-anchor" href="#status-bar-indicator" aria-label="Permalink to &quot;Status Bar Indicator&quot;">​</a></h3><p>The status bar automatically shows a task count segment when background tasks exist:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>⚙ 2 running/3 done</span></span></code></pre></div><p>Populated by <code>renderStatusBar()</code> calling <code>TaskTracker.RunningCount()</code> and <code>DoneCount()</code> in real time; shown only when at least one task exists (running or completed), taking up no status bar space when there are zero tasks.</p><h3 id="opening-the-panel" tabindex="-1">Opening the Panel <a class="header-anchor" href="#opening-the-panel" aria-label="Permalink to &quot;Opening the Panel&quot;">​</a></h3><p>Two equivalent methods:</p><table tabindex="0"><thead><tr><th>Method</th><th>Description</th></tr></thead><tbody><tr><td><code>Ctrl+T</code></td><td>Keyboard shortcut toggle (available in idle state; ignored when in conflict with running, approval, review, resume-selection, or other modals)</td></tr><tr><td><code>/tasks</code> + Enter</td><td>Slash command, with an effect identical to <code>Ctrl+T</code></td></tr></tbody></table><p>The panel is a <strong>modal view</strong>: while active, <code>taskPanelMode = true</code>, and <code>View()</code> replaces the input area with the panel content rendered by <code>renderTaskPanel()</code>; ordinary input and all other shortcuts are taken over entirely by <code>handleTaskPanelKey</code>.</p><h3 id="list-view" tabindex="-1">List View <a class="header-anchor" href="#list-view" aria-label="Permalink to &quot;List View&quot;">​</a></h3><p>The panel shows the task list by default when opened, with each line formatted as:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{● running/✓ done/✗ failed}  {agent}  {status text}  &quot;{first 48 bytes of prompt}&quot;</span></span></code></pre></div><p>The currently selected row is highlighted with <code>▶</code>. Key bindings:</p><table tabindex="0"><thead><tr><th>Key</th><th>Action</th></tr></thead><tbody><tr><td><code>↑</code> / <code>↓</code></td><td>Move the cursor</td></tr><tr><td><code>Enter</code></td><td>Enter the detail view for the selected task</td></tr><tr><td><code>Esc</code> or <code>Ctrl+T</code></td><td>Close the panel, return to normal input mode</td></tr></tbody></table><h3 id="detail-view" tabindex="-1">Detail View <a class="header-anchor" href="#detail-view" aria-label="Permalink to &quot;Detail View&quot;">​</a></h3><p>Pressing <code>Enter</code> on a selected task enters the detail view, showing that background Sub-Agent&#39;s full-process log (a deep copy of <code>TaskDetail.Log</code> obtained via <code>TaskTracker.Get(id)</code>):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>general-purpose — Done  (↑↓ to scroll, Esc to return)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Starting...</span></span>
<span class="line"><span>▸ read_file(main.go)</span></span>
<span class="line"><span>▸ bash(go vet ./...)</span></span>
<span class="line"><span>  ✗ Tool execution failed</span></span>
<span class="line"><span>Found 2 security issues...</span></span>
<span class="line"><span></span></span>
<span class="line"><span>— Final Result —</span></span>
<span class="line"><span>Suggested fixes for the following two issues...</span></span></code></pre></div><p>Log rendering is done by <code>formatTaskLog</code>, covering five event kinds: <code>SubAgentStart / SubAgentToolStart / SubAgentDelta / SubAgentToolResult (failures only) / SubAgentError</code>, with <code>SubAgentDone</code> and <code>FinalText</code> merged into a trailing &quot;Final Result&quot; block.</p><table tabindex="0"><thead><tr><th>Key</th><th>Action</th></tr></thead><tbody><tr><td><code>↑</code> / <code>↓</code></td><td>Scroll the log (<code>taskDetailScroll</code> offset)</td></tr><tr><td><code>Esc</code></td><td>Return to the list view (<code>taskDetailID = &quot;&quot;</code>)</td></tr><tr><td><code>Ctrl+T</code></td><td>Close the entire panel</td></tr></tbody></table><h3 id="live-refresh" tabindex="-1">Live Refresh <a class="header-anchor" href="#live-refresh" aria-label="Permalink to &quot;Live Refresh&quot;">​</a></h3><p>Running tasks read the <code>TaskTracker</code> snapshot directly (<code>List()</code> / <code>Get()</code>) on every panel render, requiring no subscription to notifications — the TUI main loop alone keeps the log line count (<code>LogLines</code>) updated in real time.</p><hr><h2 id="mention-invocation" tabindex="-1">@ Mention Invocation <a class="header-anchor" href="#mention-invocation" aria-label="Permalink to &quot;@ Mention Invocation&quot;">​</a></h2><h3 id="basic-usage" tabindex="-1">Basic Usage <a class="header-anchor" href="#basic-usage" aria-label="Permalink to &quot;Basic Usage&quot;">​</a></h3><p>Send in the input box using the format <code>@&lt;agent&gt; &lt;task&gt;</code>, which <strong>bypasses the main LLM&#39;s tool decision</strong> and directly invokes the specified Sub-Agent in the foreground:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>@general-purpose Investigate the timeout handling logic in internal/tools/bash.go and summarize the implementation approach</span></span></code></pre></div><p>After sending:</p><ol><li>The TUI immediately appends a user message line (<code>▶ You: @general-purpose …</code>)</li><li>A Sub-Agent name line (<code>◆ general-purpose:</code>) is appended to the scrollback</li><li><code>running = true</code>, the input box is disabled</li><li>Sub-Agent streaming progress is rendered live to <code>subAgentLines</code> (via exactly the same rendering path as the <code>task</code> tool&#39;s foreground execution)</li><li>Upon completion, the final text is appended directly to the scrollback (landing in the conversation as an assistant message), <code>running = false</code>, and the input box is re-enabled</li></ol><h3 id="tab-completion-for-sub-agent-names" tabindex="-1">Tab Completion for Sub-Agent Names <a class="header-anchor" href="#tab-completion-for-sub-agent-names" aria-label="Permalink to &quot;Tab Completion for Sub-Agent Names&quot;">​</a></h3><p>After typing <code>@</code> in the input box, pressing <code>Tab</code> auto-completes registered Sub-Agent names:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>@gen[Tab] → @general-purpose</span></span></code></pre></div><p>The completion logic is handled in <code>cycleCompletion()</code>, guarded by <code>@</code> alongside <code>/</code> slash command completion, sharing the same <code>typedPrefix / completions / completionIdx</code> cycling state; multiple <code>Tab</code> presses cycle through all matching names.</p><h3 id="ctrl-c-cancellation" tabindex="-1">Ctrl+C Cancellation <a class="header-anchor" href="#ctrl-c-cancellation" aria-label="Permalink to &quot;Ctrl+C Cancellation&quot;">​</a></h3><p>Pressing <code>Ctrl+C</code> while <code>@agent</code> is executing: <code>cancelFn()</code> cancels the derived sub-context, <code>execCtx.Done()</code> fires inside the Runner, and the sub-engine&#39;s <code>RunStream</code> exits accordingly; <code>subAgentDirectMsg{done: true, err: ctx.Err()}</code> is sent back to the TUI via the channel, <code>running = false</code>, and the input box is re-enabled.</p><h3 id="foreground-vs-background" tabindex="-1">Foreground vs Background <a class="header-anchor" href="#foreground-vs-background" aria-label="Permalink to &quot;Foreground vs Background&quot;">​</a></h3><p>The <code>@</code> syntax <strong>only supports foreground execution</strong> (<code>background=false</code>).</p><p>When background execution is needed, express the intent to the main agent in natural language (e.g. &quot;check the latest commit in the background using general-purpose&quot;), letting the main LLM decide to call the <code>task</code> tool with <code>background=true</code>, with the result appearing in the <code>/tasks</code> panel.</p><table tabindex="0"><thead><tr><th>Dimension</th><th><code>@agent task</code> (foreground direct run)</th><th>Main LLM → <code>task(background=true)</code></th></tr></thead><tbody><tr><td>Trigger</td><td>Direct user input</td><td>Main LLM tool decision</td></tr><tr><td>Main LLM involvement</td><td>No, fully bypassed</td><td>Yes, the LLM chooses the Sub-Agent and prompt</td></tr><tr><td>Execution mode</td><td>Foreground blocking, streaming progress visible</td><td>Background asynchronous, result stored in TaskTracker</td></tr><tr><td>Result landing point</td><td>Shown directly in scrollback</td><td><code>/tasks</code> panel + injected at next dispatch</td></tr><tr><td>Cancellation</td><td>Instant cancellation via <code>Ctrl+C</code></td><td>Cancelled only when baseCtx is cancelled (process shutdown)</td></tr></tbody></table><hr><h2 id="data-flow-summary" tabindex="-1">Data Flow Summary <a class="header-anchor" href="#data-flow-summary" aria-label="Permalink to &quot;Data Flow Summary&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Main agent LLM</span></span>
<span class="line"><span>    │  Decides to call the task tool</span></span>
<span class="line"><span>    ▼</span></span>
<span class="line"><span>TaskTool.Execute(ctx, args)</span></span>
<span class="line"><span>    │  Parses subagent_type / prompt / background</span></span>
<span class="line"><span>    ▼</span></span>
<span class="line"><span>Runner.Run(ctx, def, prompt, background)</span></span>
<span class="line"><span>    ├─ buildChildRegistry(def)</span></span>
<span class="line"><span>    │       ResolveTools → allowlist ∩ full set - denylist - task</span></span>
<span class="line"><span>    │       hookChain: permFileHook → denyTaskHook → dangerHook → offloadHook</span></span>
<span class="line"><span>    │</span></span>
<span class="line"><span>    ├─ providerFor(def.Model) → LLMProvider + ctxWindow</span></span>
<span class="line"><span>    │</span></span>
<span class="line"><span>    ├─ newPromptBuilder(def.SystemPrompt, workDir, def.Skills, skillsLoader)</span></span>
<span class="line"><span>    │       systemPrompt + workDir + skills body</span></span>
<span class="line"><span>    │</span></span>
<span class="line"><span>    ├─ memory.NewMemorySession(childID)   # Independent in-memory-only Session</span></span>
<span class="line"><span>    │</span></span>
<span class="line"><span>    └─ engine.NewAgentEngine(provider, childReg, workDir, opts...)</span></span>
<span class="line"><span>           │</span></span>
<span class="line"><span>           sub.RunStream(execCtx, prompt)</span></span>
<span class="line"><span>           │</span></span>
<span class="line"><span>           ▼</span></span>
<span class="line"><span>       Event stream consumption loop</span></span>
<span class="line"><span>           ├─ EventActionDelta   → emit(SubAgentDelta)   → EventSubAgent → TUI progress line</span></span>
<span class="line"><span>           ├─ EventThinkingDelta → emit(SubAgentThinking)  (not shown in TUI)</span></span>
<span class="line"><span>           ├─ EventToolStart     → emit(SubAgentToolStart) → TUI progress line</span></span>
<span class="line"><span>           ├─ EventToolResult    → emit(SubAgentToolResult)→ TUI progress line</span></span>
<span class="line"><span>           ├─ EventApprovalRequired → foreground: forwarded to parent ApprovalFunc / background: auto-denied</span></span>
<span class="line"><span>           ├─ EventError         → emit(SubAgentError) → returns error</span></span>
<span class="line"><span>           └─ EventDone          → channel closes, loop ends naturally</span></span>
<span class="line"><span>                   │</span></span>
<span class="line"><span>    Foreground: return FinalText → task tool result → parent agent context</span></span>
<span class="line"><span>    Background: tracker.AppendLog(id, update) (streaming, full-process log to memory)</span></span>
<span class="line"><span>          tracker.Finish(id, finalText, isErr)</span></span>
<span class="line"><span>              → next dispatch() → DrainCompleted() → prepended to prompt → main agent LLM</span></span></code></pre></div><hr><h2 id="wiring-example-main-go" tabindex="-1">Wiring Example (main.go) <a class="header-anchor" href="#wiring-example-main-go" aria-label="Permalink to &quot;Wiring Example (main.go)&quot;">​</a></h2><div class="language-go vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">go</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 1. Sub-Agent base tool instances (sandbox root = workDir)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">subAgentBaseTools </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> []</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">tools</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">BaseTool</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    tools.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">NewReadFileTool</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(workDir),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    tools.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">NewWriteFileTool</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(workDir),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    tools.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">NewBashTool</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(workDir),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    tools.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">NewEditFileTool</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(workDir),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    skills.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">NewUseSkillTool</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(skillsIndex),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 2. Definition registry: register the built-in general-purpose first, then load file-based definitions</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">subAgentReg </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> subagent.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">NewRegistry</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">subAgentReg.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Register</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">subagent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">SubAgentDefinition</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;general-purpose&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, Description: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;General-purpose Sub-Agent…&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, SystemPrompt: generalPurposeSystemPrompt,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Source: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;builtin&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Tools/Model left empty -&gt; inherits all tools and the model available to the parent agent</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">subAgentReg.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">LoadFromDir</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(filepath.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Join</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(workDir, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;.harness9&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;agents&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 3. Runner: hold a single global instance, read-only at runtime</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">subAgentTracker </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> subagent.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">NewTaskTracker</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">subAgentRunner </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> subagent.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">NewRunner</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">subagent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">RunnerConfig</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    BaseTools:       subAgentBaseTools,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    SharedHooks:     []</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">hooks</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ToolHook</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{dangerHook, offloadHook},</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    SettingsPath:    settingsPath,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    SkillsIndex:     skillsIndex,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    WorkDir:         workDir,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    DefaultMaxTurns: agentMaxTurns, </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// = main agent&#39;s 50, Sub-Agent matches the main agent</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ToolTimeout:     </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">60</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> time.Second,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ProviderFor:     </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">func</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">model</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) (</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">provider</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">LLMProvider</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">error</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) { </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    CompactorFor:    </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">func</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">p</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> provider</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">LLMProvider</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctxWin</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">memory</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Compactor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    BaseCtx:         ctx,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 4. Register the task tool into the parent agent&#39;s registry</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">taskTool </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> subagent.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">NewTaskTool</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(subAgentReg, subAgentRunner, subAgentTracker)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">registry.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Register</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(taskTool)</span></span></code></pre></div><hr><h2 id="file-index" tabindex="-1">File Index <a class="header-anchor" href="#file-index" aria-label="Permalink to &quot;File Index&quot;">​</a></h2><table tabindex="0"><thead><tr><th>File</th><th>Responsibility</th></tr></thead><tbody><tr><td><code>internal/subagent/definition.go</code></td><td><code>SubAgentDefinition</code> struct, <code>Validate</code>, <code>ResolveTools</code></td></tr><tr><td><code>internal/subagent/registry.go</code></td><td><code>Registry</code>: <code>Register</code> / <code>Get</code> / <code>List</code></td></tr><tr><td><code>internal/subagent/frontmatter.go</code></td><td><code>parseAgentFile</code>: YAML frontmatter parsing</td></tr><tr><td><code>internal/subagent/loader.go</code></td><td><code>Registry.LoadFromDir</code>: file-based definition loading</td></tr><tr><td><code>internal/subagent/prompt.go</code></td><td><code>promptBuilder</code>: system prompt + skills + workDir assembly</td></tr><tr><td><code>internal/subagent/tracker.go</code></td><td><code>TaskTracker</code>: single source of truth for background tasks (full-process log + result injection)</td></tr><tr><td><code>internal/subagent/runner.go</code></td><td><code>Runner</code>: builds the isolated sub-engine + executes + forwards events</td></tr><tr><td><code>internal/subagent/task_tool.go</code></td><td><code>TaskTool</code>: <code>task</code> tool implementation (foreground / background)</td></tr><tr><td><code>internal/schema/subagent.go</code></td><td><code>SubAgentUpdate</code> / <code>SubAgentUpdateKind</code> type definitions</td></tr><tr><td><code>internal/hooks/subagent_progress.go</code></td><td><code>SubAgentProgressFunc</code>: context injection/extraction</td></tr><tr><td><code>internal/engine/stream.go</code></td><td><code>EventSubAgent</code>, <code>EventApprovalRequired</code>, progress sink injection</td></tr><tr><td><code>cmd/harness9/main.go</code></td><td>Complete wiring: built-in Sub-Agent registration, Runner construction, task tool registration</td></tr><tr><td><code>cmd/harness9/tui_update.go</code></td><td><code>EventSubAgent</code> handling, <code>TaskTracker.DrainCompleted</code> injection in <code>dispatch()</code>, <code>dispatchMention</code> (@ foreground direct run), <code>handleTaskPanelKey</code> (task panel key bindings)</td></tr><tr><td><code>cmd/harness9/tui_view.go</code></td><td><code>renderSubAgentProgress()</code> (dark-cyan progress block), <code>renderTaskPanel()</code> (panel list/detail), background task count in <code>renderStatusBar()</code></td></tr></tbody></table>`,113)])])}const u=t(i,[["render",o]]);export{g as __pageData,u as default};
