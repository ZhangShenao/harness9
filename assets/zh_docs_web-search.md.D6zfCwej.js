import{_ as e,o as t,c as s,a2 as n}from"./chunks/framework.CNfFRqmr.js";const g=JSON.parse('{"title":"网页搜索与抓取技术方案","description":"harness9 通过 web_search 和 web_fetch 两个内置工具赋予 Agent 实时访问互联网的能力，无需任何 API Key，内置生产级 SSRF 防护。","frontmatter":{"title":"网页搜索与抓取技术方案","description":"harness9 通过 web_search 和 web_fetch 两个内置工具赋予 Agent 实时访问互联网的能力，无需任何 API Key，内置生产级 SSRF 防护。"},"headers":[],"relativePath":"zh/docs/web-search.md","filePath":"zh/docs/web-search.md"}'),d={name:"zh/docs/web-search.md"};function o(p,a,l,i,c,r){return t(),s("div",null,[...a[0]||(a[0]=[n(`<h1 id="网页搜索与抓取技术方案" tabindex="-1">网页搜索与抓取技术方案 <a class="header-anchor" href="#网页搜索与抓取技术方案" aria-label="Permalink to &quot;网页搜索与抓取技术方案&quot;">​</a></h1><p>harness9 通过 <code>web_search</code> 和 <code>web_fetch</code> 两个内置工具赋予 Agent 实时访问互联网的能力，无需任何 API Key，内置生产级 SSRF 防护。</p><hr><h2 id="_1-设计决策" tabindex="-1">1. 设计决策 <a class="header-anchor" href="#_1-设计决策" aria-label="Permalink to &quot;1. 设计决策&quot;">​</a></h2><h3 id="为什么不做成-mcp-工具" tabindex="-1">为什么不做成 MCP 工具 <a class="header-anchor" href="#为什么不做成-mcp-工具" aria-label="Permalink to &quot;为什么不做成 MCP 工具&quot;">​</a></h3><p>DeepAgents 和 OpenCode 将网页能力完全外包给 MCP 服务器，零配置即零能力——用户必须自行安装配置 Tavily 或 Brave MCP。harness9 的哲学是<strong>开箱即用</strong>：DuckDuckGo 无需任何 Key，<code>go-readability</code> 是纯 Go 库，整个能力链条不依赖任何外部平台账号。</p><h3 id="工具原子化-深度搜索在-llm-层" tabindex="-1">工具原子化，深度搜索在 LLM 层 <a class="header-anchor" href="#工具原子化-深度搜索在-llm-层" aria-label="Permalink to &quot;工具原子化，深度搜索在 LLM 层&quot;">​</a></h3><p>工具只提供两个原子能力：搜索（返回结果链接）和抓取（返回页面内容）。是否继续跟踪链接、是否扩展搜索词、何时停止——这些决策完全交给 LLM 在 ReAct 循环中完成。这是六个主流框架的共同设计选择，工具层无需也不应内置搜索树遍历逻辑。</p><h3 id="为什么要注入当前日期" tabindex="-1">为什么要注入当前日期 <a class="header-anchor" href="#为什么要注入当前日期" aria-label="Permalink to &quot;为什么要注入当前日期&quot;">​</a></h3><p>LLM 的知识截止日期（training cutoff）与当前时间存在差距。不注入日期时，Agent 在搜索时倾向于使用训练截止年份，导致搜索&quot;最新版本&quot;时拿到过期结果。harness9 在 System Prompt 基础段注入 <code>time.Now().Format(&quot;2006-01-02&quot;)</code> 实时日期，Agent 每次会话自动感知当天日期，无需额外工具调用。</p><hr><h2 id="_2-工具接口" tabindex="-1">2. 工具接口 <a class="header-anchor" href="#_2-工具接口" aria-label="Permalink to &quot;2. 工具接口&quot;">​</a></h2><h3 id="web-search" tabindex="-1"><code>web_search</code> <a class="header-anchor" href="#web-search" aria-label="Permalink to &quot;\`web_search\`&quot;">​</a></h3><p>搜索互联网，返回标题、URL 和摘要列表。</p><table tabindex="0"><thead><tr><th>参数</th><th>类型</th><th style="text-align:center;">必填</th><th>默认值</th><th>说明</th></tr></thead><tbody><tr><td><code>query</code></td><td>string</td><td style="text-align:center;">是</td><td>—</td><td>搜索词，英文效果更好</td></tr><tr><td><code>max_results</code></td><td>int</td><td style="text-align:center;">否</td><td>5</td><td>返回条数，范围 1–10</td></tr></tbody></table><p><strong>输出格式：</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[1] Go 1.25 Release Notes</span></span>
<span class="line"><span>URL: https://go.dev/doc/go1.25</span></span>
<span class="line"><span>摘要: Go 1.25 introduces range-over-func, improved type inference...</span></span>
<span class="line"><span></span></span>
<span class="line"><span>[2] What&#39;s new in Go 1.25</span></span>
<span class="line"><span>URL: https://blog.golang.org/go1.25</span></span>
<span class="line"><span>摘要: The Go team is pleased to announce Go 1.25...</span></span></code></pre></div><h3 id="web-fetch" tabindex="-1"><code>web_fetch</code> <a class="header-anchor" href="#web-fetch" aria-label="Permalink to &quot;\`web_fetch\`&quot;">​</a></h3><p>抓取指定 URL，返回 Markdown 格式的主要内容。</p><table tabindex="0"><thead><tr><th>参数</th><th>类型</th><th style="text-align:center;">必填</th><th>默认值</th><th>说明</th></tr></thead><tbody><tr><td><code>url</code></td><td>string</td><td style="text-align:center;">是</td><td>—</td><td>目标 URL（http/https）</td></tr><tr><td><code>max_chars</code></td><td>int</td><td style="text-align:center;">否</td><td>8000</td><td>返回字符上限，最大 32000</td></tr></tbody></table><p><strong>输出格式：</strong></p><div class="language-markdown vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">markdown</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;"># Go 1.25 Release Notes</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">&gt; 来源：https://go.dev/doc/go1.25</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">## New Features</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">...主内容（Markdown 格式）...</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[内容已截断，已显示前 8000 字符]   ← 仅超出限制时出现</span></span></code></pre></div><hr><h2 id="_3-ssrf-防护-web-safety-go" tabindex="-1">3. SSRF 防护（<code>web_safety.go</code>） <a class="header-anchor" href="#_3-ssrf-防护-web-safety-go" aria-label="Permalink to &quot;3. SSRF 防护（\`web_safety.go\`）&quot;">​</a></h2><p>所有 HTTP 请求在发出前经过 <code>isSafeURL</code> 校验，类比 <code>safe_path.go</code> 对文件系统路径的沙箱保护。</p><h3 id="检查链" tabindex="-1">检查链 <a class="header-anchor" href="#检查链" aria-label="Permalink to &quot;检查链&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>URL 输入</span></span>
<span class="line"><span>  │</span></span>
<span class="line"><span>  ▼</span></span>
<span class="line"><span>① scheme 校验：必须是 http 或 https，拒绝 ftp / file 等</span></span>
<span class="line"><span>  │</span></span>
<span class="line"><span>  ▼</span></span>
<span class="line"><span>② userinfo 拒绝：URL 中含 user:pass@... 格式一律拒绝</span></span>
<span class="line"><span>  │</span></span>
<span class="line"><span>  ▼</span></span>
<span class="line"><span>③ DNS 解析：net.LookupHost(hostname)</span></span>
<span class="line"><span>  │</span></span>
<span class="line"><span>  ├── 解析失败 → fail-closed（拒绝请求）</span></span>
<span class="line"><span>  │              防止 DNS rebinding 攻击的第一道屏障</span></span>
<span class="line"><span>  │</span></span>
<span class="line"><span>  ▼</span></span>
<span class="line"><span>④ IP 段检查：对每个解析出的 IP 地址检查</span></span></code></pre></div><h3 id="永久禁止的-ip-范围" tabindex="-1">永久禁止的 IP 范围 <a class="header-anchor" href="#永久禁止的-ip-范围" aria-label="Permalink to &quot;永久禁止的 IP 范围&quot;">​</a></h3><table tabindex="0"><thead><tr><th>网段</th><th>说明</th><th style="text-align:center;">可配置</th></tr></thead><tbody><tr><td><code>169.254.0.0/16</code></td><td>链路本地 + AWS/Azure/GCP metadata 端点</td><td style="text-align:center;">永久禁止</td></tr><tr><td><code>127.0.0.0/8</code></td><td>IPv4 loopback</td><td style="text-align:center;">默认禁止</td></tr><tr><td><code>::1</code></td><td>IPv6 loopback（<code>net.IPv6loopback</code> 独立检查）</td><td style="text-align:center;">默认禁止</td></tr><tr><td><code>10.0.0.0/8</code></td><td>RFC1918 私网</td><td style="text-align:center;">默认禁止</td></tr><tr><td><code>172.16.0.0/12</code></td><td>RFC1918 私网</td><td style="text-align:center;">默认禁止</td></tr><tr><td><code>192.168.0.0/16</code></td><td>RFC1918 私网</td><td style="text-align:center;">默认禁止</td></tr><tr><td><code>100.64.0.0/10</code></td><td>CGNAT</td><td style="text-align:center;">默认禁止</td></tr><tr><td><code>fe80::/10</code></td><td>IPv6 链路本地（等价于 IPv4 169.254.0.0/16）</td><td style="text-align:center;">默认禁止</td></tr><tr><td><code>fc00::/7</code></td><td>IPv6 唯一本地地址 ULA（等价于 IPv4 RFC1918 私网）</td><td style="text-align:center;">默认禁止</td></tr></tbody></table><h3 id="为什么-dns-解析后二次检查-ip" tabindex="-1">为什么 DNS 解析后二次检查 IP <a class="header-anchor" href="#为什么-dns-解析后二次检查-ip" aria-label="Permalink to &quot;为什么 DNS 解析后二次检查 IP&quot;">​</a></h3><p>攻击者可以让域名先解析到公网 IP 通过校验，然后在 DNS TTL 窗口内将解析切换到内网地址（DNS rebinding）。先解析再检查 IP 可以斩断这条路——<code>isSafeURL</code> 拿到的是 DNS 查询时刻的真实 IP，不是 hostname。</p><h3 id="ipv4-mapped-ipv6-规范化" tabindex="-1">IPv4-mapped IPv6 规范化 <a class="header-anchor" href="#ipv4-mapped-ipv6-规范化" aria-label="Permalink to &quot;IPv4-mapped IPv6 规范化&quot;">​</a></h3><p><code>::ffff:169.254.169.254</code> 是 <code>169.254.169.254</code> 的 IPv6 表示形式。代码通过 <code>ip.To4()</code> 将 IPv4-mapped IPv6 规范化为 IPv4，确保 IPv6 表示的内网地址不会绕过 CIDR 检查。</p><h3 id="重定向链-ssrf-防护" tabindex="-1">重定向链 SSRF 防护 <a class="header-anchor" href="#重定向链-ssrf-防护" aria-label="Permalink to &quot;重定向链 SSRF 防护&quot;">​</a></h3><p><code>web_fetch</code> 的 <code>CheckRedirect</code> 回调在每次重定向后对新 URL 重新调用 <code>isSafeURL</code>，防止 open redirect → 内网的攻击路径：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>外网 URL → SSRF check ✓ → 服务端 301 → 内网 URL → SSRF check ✗（拒绝）</span></span></code></pre></div><hr><h2 id="_4-html-内容提取管线-web-content-go" tabindex="-1">4. HTML 内容提取管线（<code>web_content.go</code>） <a class="header-anchor" href="#_4-html-内容提取管线-web-content-go" aria-label="Permalink to &quot;4. HTML 内容提取管线（\`web_content.go\`）&quot;">​</a></h2><p><code>web_fetch</code> 抓取到 HTML 后，经过三级管线转换为 LLM 友好的 Markdown：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>HTML body (io.Reader)</span></span>
<span class="line"><span>    │</span></span>
<span class="line"><span>    ▼</span></span>
<span class="line"><span>① 大小限制（1MB）</span></span>
<span class="line"><span>    │ 超出 → 返回 error</span></span>
<span class="line"><span>    ▼</span></span>
<span class="line"><span>② go-readability.FromReader()</span></span>
<span class="line"><span>    │ 成功 → Article{Title, Content(HTML)}</span></span>
<span class="line"><span>    │ 失败 → 直接走 fallback</span></span>
<span class="line"><span>    ▼</span></span>
<span class="line"><span>③ html-to-markdown.Convert(Article.Content)</span></span>
<span class="line"><span>    │ 成功 → Markdown 字符串</span></span>
<span class="line"><span>    │ 失败 → 使用 Article.TextContent（readability 提取的纯文本）</span></span>
<span class="line"><span>    ▼</span></span>
<span class="line"><span>④ assemblePage(rawURL, title, content, maxChars)</span></span>
<span class="line"><span>    │ 组装输出：# 标题 + &gt; 来源 + 内容</span></span>
<span class="line"><span>    │ 超出 maxChars → 截断 + 追加标记</span></span>
<span class="line"><span>    ▼</span></span>
<span class="line"><span>Markdown 输出</span></span></code></pre></div><p><strong>Fallback 路径</strong>：当 <code>go-readability</code> 完全失败（如极简 HTML 或内容为空），使用 <code>golang.org/x/net/html</code> tokenizer 做简单文本提取——跳过 <code>&lt;script&gt;</code> 和 <code>&lt;style&gt;</code> 标签内容，拼接可见文本节点。这个 tokenizer 是 Go 标准库范畴的间接依赖，零额外引入。</p><h3 id="大小常量" tabindex="-1">大小常量 <a class="header-anchor" href="#大小常量" aria-label="Permalink to &quot;大小常量&quot;">​</a></h3><table tabindex="0"><thead><tr><th>常量</th><th>值</th><th>说明</th></tr></thead><tbody><tr><td><code>defaultMaxChars</code></td><td>8,000</td><td><code>max_chars</code> 参数默认值（约 2000 token）</td></tr><tr><td><code>hardMaxChars</code></td><td>32,000</td><td><code>max_chars</code> 参数上限（约 8000 token）</td></tr><tr><td><code>maxHTMLBodySize</code></td><td>1,048,576 (1MB)</td><td>HTTP 响应体读取上限</td></tr></tbody></table><hr><h2 id="_5-duckduckgo-搜索后端-web-search-go" tabindex="-1">5. DuckDuckGo 搜索后端（<code>web_search.go</code>） <a class="header-anchor" href="#_5-duckduckgo-搜索后端-web-search-go" aria-label="Permalink to &quot;5. DuckDuckGo 搜索后端（\`web_search.go\`）&quot;">​</a></h2><h3 id="为什么选择-duckduckgo" tabindex="-1">为什么选择 DuckDuckGo <a class="header-anchor" href="#为什么选择-duckduckgo" aria-label="Permalink to &quot;为什么选择 DuckDuckGo&quot;">​</a></h3><p>调研六个主流框架的搜索后端选择：DuckDuckGo 是唯一一个<strong>所有框架都支持、无 API Key 要求</strong>的后端，且其 HTML 端点（<code>html.duckduckgo.com/html/</code>）返回纯 HTML——不需要 JavaScript 渲染，标准 HTTP 客户端即可处理。</p><h3 id="请求流程" tabindex="-1">请求流程 <a class="header-anchor" href="#请求流程" aria-label="Permalink to &quot;请求流程&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>POST https://html.duckduckgo.com/html/</span></span>
<span class="line"><span>Content-Type: application/x-www-form-urlencoded</span></span>
<span class="line"><span>User-Agent: harness9/1.0</span></span>
<span class="line"><span>Body: q=&lt;url-encoded-query&gt;</span></span></code></pre></div><p>超时控制：</p><ul><li><strong>Dial 超时</strong>：10s（TCP 握手阶段，<code>net.Dialer.Timeout</code>）</li><li><strong>请求超时</strong>：20s（Context deadline，含 DNS + dial + 读取响应）</li></ul><p>双重超时解决了 <code>http.DefaultClient</code> 的一个已知问题：仅用 context timeout 时，极慢的 TCP 握手可能无法被及时中断；独立的 dial timeout 保证连接建立阶段的响应性。</p><h3 id="html-解析" tabindex="-1">HTML 解析 <a class="header-anchor" href="#html-解析" aria-label="Permalink to &quot;HTML 解析&quot;">​</a></h3><p><code>golang.org/x/net/html</code>（已有间接依赖，零新增）的 <code>html.Parse()</code> 构建完整 DOM 树，递归遍历提取：</p><table tabindex="0"><thead><tr><th>选择器</th><th>目标内容</th></tr></thead><tbody><tr><td><code>div.result</code>（排除 <code>div.result--more</code>）</td><td>单条搜索结果容器</td></tr><tr><td><code>a.result__a</code></td><td>标题文本 + href</td></tr><tr><td><code>a.result__snippet</code></td><td>摘要文本</td></tr></tbody></table><h3 id="ddg-重定向-url-解码" tabindex="-1">DDG 重定向 URL 解码 <a class="header-anchor" href="#ddg-重定向-url-解码" aria-label="Permalink to &quot;DDG 重定向 URL 解码&quot;">​</a></h3><p>DuckDuckGo 的链接使用重定向格式：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>href=&quot;/l/?uddg=https%3A%2F%2Fexample.com%2Fpath&amp;rut=...&quot;</span></span></code></pre></div><p><code>decodeUDDG()</code> 从 <code>uddg</code> 参数提取真实目标 URL，LLM 拿到的是可直接传入 <code>web_fetch</code> 的最终 URL。</p><h3 id="可测试性设计" tabindex="-1">可测试性设计 <a class="header-anchor" href="#可测试性设计" aria-label="Permalink to &quot;可测试性设计&quot;">​</a></h3><p><code>WebSearchTool</code> 结构体的 <code>backendURL</code> 字段默认指向 DDG 端点，测试中可替换为 <code>httptest.NewServer</code> 的本地地址，无需真实网络请求：</p><div class="language-go vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">go</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 生产</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">tool </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> NewWebSearchTool</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// backendURL = &quot;https://html.duckduckgo.com/html/&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 测试</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">tool </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &amp;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">WebSearchTool</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{backendURL: server.URL}</span></span></code></pre></div><p>同理，<code>WebFetchTool</code> 的 <code>safetyCheck</code> 字段在测试中替换为 no-op，允许访问 127.0.0.1 的 httptest 服务器而不触发 SSRF 拦截。</p><hr><h2 id="_6-当前日期注入-internal-context-builder-go" tabindex="-1">6. 当前日期注入（<code>internal/context/builder.go</code>） <a class="header-anchor" href="#_6-当前日期注入-internal-context-builder-go" aria-label="Permalink to &quot;6. 当前日期注入（\`internal/context/builder.go\`）&quot;">​</a></h2><p><code>DefaultPromptBuilder.Build()</code> 在基础 System Prompt 中注入当天日期：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>工作目录：/path/to/project</span></span>
<span class="line"><span>当前日期：2026-06-12</span></span></code></pre></div><p>每次 <code>Build()</code> 调用时实时执行 <code>time.Now().Format(&quot;2006-01-02&quot;)</code>，保证 Agent 在每个会话中感知正确的日期，搜索时自然带入当前年份，避免因训练截止日期偏差导致的陈旧搜索结果。</p><hr><h2 id="_7-模块结构" tabindex="-1">7. 模块结构 <a class="header-anchor" href="#_7-模块结构" aria-label="Permalink to &quot;7. 模块结构&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>internal/tools/</span></span>
<span class="line"><span>├── web_safety.go        # SSRF 防护：isSafeURL（检查链 + blockedCIDRs）</span></span>
<span class="line"><span>├── web_safety_test.go   # 19 个测试用例：IP 段 / scheme / DNS fail-closed / IPv6 / IPv6 ULA / IPv6 链路本地</span></span>
<span class="line"><span>├── web_content.go       # HTML→Markdown 管线：extractContent + extractPlainText + assemblePage（UTF-8 安全截断）</span></span>
<span class="line"><span>├── web_content_test.go  # 5 个测试用例：基础转换 / 截断 / fallback / 超大 body / UTF-8 安全截断</span></span>
<span class="line"><span>├── web_fetch.go         # WebFetchTool：HTTP GET + Content-Type 分支</span></span>
<span class="line"><span>├── web_fetch_test.go    # 6 个测试用例：SSRF / HTML / text / unsupported / 空URL / 4xx</span></span>
<span class="line"><span>├── web_search.go        # WebSearchTool：DuckDuckGo POST + DOM 解析 + decodeUDDG</span></span>
<span class="line"><span>└── web_search_test.go   # 6 个测试用例：DDG 解析 / max_results / URL 解码 / Execute / 空结果</span></span></code></pre></div><p><strong>依赖关系：</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>web_fetch.go ──────┐</span></span>
<span class="line"><span>                   ├──→ web_safety.go (isSafeURL)</span></span>
<span class="line"><span>web_search.go ─────┘</span></span>
<span class="line"><span>     │</span></span>
<span class="line"><span>     └──→ web_content.go (extractContent)  ──→ go-readability</span></span>
<span class="line"><span>                                           ──→ html-to-markdown</span></span>
<span class="line"><span>                                           ──→ golang.org/x/net/html (已有依赖)</span></span></code></pre></div><hr><h2 id="_8-配置与使用" tabindex="-1">8. 配置与使用 <a class="header-anchor" href="#_8-配置与使用" aria-label="Permalink to &quot;8. 配置与使用&quot;">​</a></h2><p>两个工具随 harness9 启动自动注册，主 Agent 和所有 Sub-Agent 均可使用，无需任何配置。</p><p><strong>典型搜索流程：</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>用户：搜索一下 Go 1.25 的新特性</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Agent:</span></span>
<span class="line"><span>  1. web_search(&quot;Go 1.25 release notes&quot;) → 获取结果列表</span></span>
<span class="line"><span>  2. web_fetch(&quot;https://go.dev/doc/go1.25&quot;) → 获取详细内容</span></span>
<span class="line"><span>  3. 基于抓取内容回答用户</span></span></code></pre></div><p><strong>Sub-Agent 委派（研究场景）：</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>@general-purpose 搜索 LangGraph 最新版本的 checkpoint 机制并总结关键 API</span></span></code></pre></div><p><code>general-purpose</code> 子代理继承父代理的全部工具，包括 <code>web_search</code> 和 <code>web_fetch</code>，可独立完成多轮搜索 + 抓取 + 总结，最终只将结论回传给主上下文。</p><hr><h2 id="_9-限制与注意事项" tabindex="-1">9. 限制与注意事项 <a class="header-anchor" href="#_9-限制与注意事项" aria-label="Permalink to &quot;9. 限制与注意事项&quot;">​</a></h2><ul><li><strong>DuckDuckGo 限速</strong>：无官方 SLA，高频调用可能触发临时封锁。如需稳定高频搜索，建议通过环境变量配置 Brave Search 或 Tavily（待 P2 后端接口实现后支持）</li><li><strong>JavaScript 渲染页面</strong>：<code>web_fetch</code> 使用纯 HTTP 请求，不执行 JavaScript，动态渲染的 SPA 内容无法抓取</li><li><strong><code>go-readability</code> 废弃声明</strong>：当前使用 <code>github.com/go-shiori/go-readability</code>，作者已标注 deprecated，建议未来迁移至 <code>codeberg.org/readeck/go-readability/v2</code></li><li><strong>1MB 大小限制</strong>：超过 1MB 的页面会被拒绝处理并返回 error</li></ul><hr><h2 id="_10-参考实现" tabindex="-1">10. 参考实现 <a class="header-anchor" href="#_10-参考实现" aria-label="Permalink to &quot;10. 参考实现&quot;">​</a></h2><table tabindex="0"><thead><tr><th>框架</th><th>策略</th><th>关键特点</th></tr></thead><tbody><tr><td>OpenHarness</td><td>内置极简</td><td>DuckDuckGo HTML 解析 + 自实现 HTML→纯文本，12000 字符截断</td></tr><tr><td>OpenClaw</td><td>插件化混合</td><td>Mozilla Readability + htmlToMarkdown，5+ 搜索后端级联 Fallback</td></tr><tr><td>HermesAgent</td><td>全功能</td><td>8 个后端，LLM 内容压缩管线（Gemini Flash，支持 2M 字符分块）</td></tr><tr><td>Claude Agent SDK</td><td>官方内置</td><td><code>WebSearch</code>/<code>WebFetch</code> 原生工具，research-agent 示例展示 Sub-Agent 并行研究模式</td></tr></tbody></table><p>详细调研见 <code>docs/技术调研/web-search-capability.md</code>（本地调研文档，不随仓库发布）。</p>`,88)])])}const b=e(d,[["render",o]]);export{g as __pageData,b as default};
